development:
    adapter: mysql2
    encoding: utf8
    host: localhost
    database: yammerlike_dev
    pool: 5
    username: root
    password:
    socket: /tmp/mysql.sock

test:
    adapter: mysql2
    encoding: utf8
    host: localhost
    database: yammerlike_tst
    pool: 5
    username: root
    password:
    socket: /tmp/mysql.sock

production:
    adapter: mysql2
    encoding: utf8
    host: localhost
    database: yammerlike_prd
    pool: 5
    username: root
    password:
    socket: /tmp/mysql.sock
